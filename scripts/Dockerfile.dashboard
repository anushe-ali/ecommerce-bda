# scripts/Dockerfile.dashboard
FROM python:3.10-slim

# Set working directory
WORKDIR /app

# Copy requirements and install
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy all scripts
COPY . .

# Expose port
EXPOSE 8050

# Run the Dash app
CMD ["python", "dashboard.py"]
